Class
	+ attribute : ( thuộc tính = biến toàn cục )
	+ method : ( phương thức = hàm )
		. biến cục bộ
		
KDL của biến
	+ KDL nguyên thủy : int, char, byte, ...
	+ KDL đối tượng
		. String, Integer, Double, ...
		. Item, Employee, ...
	+ Heap / Stack
	
Operator 
	+ prefix/suffix 
	+ = == && || !
	+ ? :
	
IO / Random
	
Control
	+ if/else
	+ switch case
	+ for / while/ do-while
	+ break, continue