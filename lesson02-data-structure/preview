class
	+ attribute (thuộc tính = biến tòan cục)
	+ method (phương thức = hàm )
	  . biến cục bộ

KDL của biến
	+ nguyên thủy: int, char, byte, double, float
	+ đối tượng:
		+ String, Integer, Double
		+ Item , Employee
	+ heap/Stack
	
Toán tử
	+ prefix/ suffix
	+ = == && || !
	+ ? :
	
Nhập xuất - Random


Cấu trúc điều kiện
	+ if else, switch case
	+ for, while, do while
	+ break, continue
	
+ Access modifier: public/ private
+ Static