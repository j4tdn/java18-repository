Class
	+ attribute(thuộc tính = biến toàn cục)
	+ method(phương thức = hàm)
		. biến cục bộ
		
KDL của biến
	+ KDL nguyên thủy: int, char, byte, double, float
	+ Đối tượng:
		. String, Integer, Double
		. Item, Employee
	+ HEAP/STACK
	
Toán tử
	+ prefix/suffix
	+ = ++ && || !
	+ ? :

Nhập xuất - Random

Cấu trúc điều kiện
	+ if else, switch case
	+ for, while, do while
	+ break, continue
	
+ Access modifier: public/private
+ static