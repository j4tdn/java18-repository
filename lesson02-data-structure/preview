Class 
    + attribute (thuộc tính = biến toàn cục)
    + method (phương thức = hàm)
        - biến cục bộ
        
KDL của biến
    + Kiểu nguyên thủy:int, char, byte, double, float
    
    + Kiểu đối tượng:
         - String, Integer, Double
         - Item, employee.. (tự tạo)
    + HEAP/ STACK
    
Operate ( toán tử)
    + prefix/ suffix
    + = == && || !
    + ? :
    
Nhập xuất - Random

Cấu trúc điều kiện/khiển
    + if else, switch case
    + for, whilt, do while
    + break, continue
    
Access modifier: public/ private
static