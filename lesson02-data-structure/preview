Class
	+ attribute ( Thuộc tính =  biến toàn cục)
	+ method(phương thức = hàm)
		.biên cục bộ
		
KDL của biến
	+ Nguyên thuỷ: int, char, byte, double, float
	+ đối tượng:
		+String, Integer, Double
		+Item, Employee
	+ Heap/Stack
	
	
Toán tử
	+ prefix/surffix
	+ =  ==  && || !
	+ ? :
	
	
Nhập xuất - Random

cấu trúc điều kiện/khiển
	+if else , switch, case
	+for, while, do while
	+break, continue
	
+ Access modifier: public/private
+ static